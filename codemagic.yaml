app_id: com.claw.control

environment:
  group_vars:
    - id: DEVELOPMENT_TEAM
      value: ""  # ⚠️ 填入你的 Apple Team ID（Personal Team 也有）
  node: 22

workflows:
  ios-build:
    name: Build iOS (ClawControl)
    max_build_duration: 120
    environment:
      node: 22
      xcode: latest
      cocoapods: default
    scripts:
      # 1. 安装 npm 依赖
      - name: Install dependencies
        script: |
          npm ci

      # 2. 构建 Web 应用
      - name: Build web app
        script: |
          npm run mobile:build

      # 3. 添加 iOS 平台（Capacitor 8+ 默认 SPM，需显式指定 CocoaPods）
      - name: Add iOS platform
        script: |
          npx cap add ios --packagemanager CocoaPods

      # 4. 同步 Web 产物到 iOS 原生项目
      - name: Sync Capacitor iOS
        script: |
          npx cap sync ios

      # 5. 安装 CocoaPods 依赖
      - name: Install CocoaPods
        script: |
          cd ios/App && pod install && cd ../..

      # 6. 生成 iOS 图标和启动屏资源
      - name: Generate iOS assets
        script: |
          npx @capacitor/assets generate --ios --splashBackgroundColor "#06080a" --iconBackgroundColor "#06080a"

      # 7. 修复 iOS 启动屏样式
      - name: Fix iOS splash
        script: |
          if [ -f "scripts/fix-ios-splash.js" ]; then node scripts/fix-ios-splash.js; fi

      # 8. 创建 ExportOptions.plist（无签名 ad-hoc 导出）
      - name: Create ExportOptions.plist
        script: |
          mkdir -p ios
          cat > ios/ExportOptions.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>ad-hoc</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>signingCertificate</key>
              <string>-</string>
          </dict>
          </plist>
          EOF

      # 9. 构建 iOS 项目并导出 IPA（-archive 是默认 action，无需显式写）
      - name: Build and export IPA
        script: |
          cd ios/App

          # 先用 xcodebuild -showBuildSettings 确认 scheme 列表
          xcodebuild -workspace App.xcworkspace -list

          # 构建 archive
          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -archivePath App.xcarchive \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

          # 导出 IPA（无签名模式）
          mkdir -p ../build
          xcodebuild -exportArchive \
            -archivePath App.xcarchive \
            -exportPath ../build \
            -exportOptionsPlist ../ExportOptions.plist

    artifacts:
      - build/App.ipa
      - build/App.xcarchive
